# CareerGraph frontend

React + TypeScript + Vite app for CareerGraph. See the root [README](../README.md) for the full
project layout, architecture, and run instructions.

```
cd frontend
npm install
npm run dev
```

Open `http://localhost:5173`. Requires the backend running on port 8000 (see root README).
